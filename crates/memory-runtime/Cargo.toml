[package]
name = "memory-runtime"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
memory-core = { path = "../memory-core" }
thiserror = "2.0"
tokio = { version = "1.41", features = ["rt", "sync"] }

[features]
indexer = []
webhooks = []

[lints]
workspace = true
